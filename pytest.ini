[pytest]
testpaths = scenarios/MockUI/tests
pythonpath = scenarios/MockUI/src src
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Per-test tmp_path folders live in the gitignored build/ dir; pytest empties this root each run.
addopts = --basetemp=build/pytest
